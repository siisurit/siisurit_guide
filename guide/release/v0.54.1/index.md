# v0.54.1

## Fixes

The management command `make_demo_project` now stops generating work entries when the `--weeks` limit ends in the past.

The text on the dashboard is now properly translated:

> You are currently assigned to N tasks.
