# v0.53.0

## Features

The dashboard shows some possibly interesting information now.

## Improvements

When [make_users](../../admin/commands/make_users.md) cannot process a CSV row because of low level errors (before even getting to row validation), the error message is more precise now.

### Fixes

The numbers on tracker cards are now correct and consistent with the numbers on the respective project card.

Task cards now show the fractional parts of work in hours.
